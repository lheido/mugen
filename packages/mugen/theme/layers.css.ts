import { layer } from "@vanilla-extract/css";

export const reset = layer("reset");
export const base = layer("base");
export const mugen = layer("mugen");
export const utilities = layer("utilities");
